from setuptools import setup, find_packages, Extension

setup(name='moving',
      version='1.0.0',
      description='Tools for simulating optomechanical systems in a moving basis.',
      packages=find_packages())